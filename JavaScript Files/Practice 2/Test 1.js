let person = {
    name: "Ahiezer",
    age: 20,
    country: "Philippines"
}

function logData(){
    return person.name + " is " + person.age + " years old and lives in " + person.country
}

console.log(logData())